Сделаю точечную правку демо-страницы без изменения компонентов библиотеки.

План:
1. В `src/routes/index.tsx` заменить основную сетку демо-секций с двухколоночной на одноколоночную:
   - было: `grid gap-5 lg:grid-cols-2`
   - станет: `grid gap-5`
2. Внутреннюю сетку внутри секции `Select` оставлю как есть, потому что это не раскладка всей демо-страницы, а локальное сравнение single/multiple select. Если нужно строго вообще всё в один столбец, дополнительно уберу `sm:grid-cols-2` и там.
3. Никаких скриншотов делать не буду.
4. После правки проверю файл на отсутствие лишних изменений и синтаксических ошибок по месту.

Технически изменение затронет только `src/routes/index.tsx`.