Проблема понятна: сейчас `Carousel` рендерит служебные элементы Bootstrap/Reactstrap неправильно. Управление вставлено внутрь `<Carousel>` до слайдов и с не теми CSS-классами из CSS Modules, а `CarouselCaption` дублирует текст — из-за этого в демо видны все слайды столбцом и подписи повторяются.

План исправления:

1. Исправить структуру компонента `src/lib/components/Carousel/Carousel.tsx`
   - Рендерить только `CarouselItem` внутри `CarouselReactstrap` как ожидает Reactstrap.
   - Перенести стрелки и индикаторы в корректные места или оставить стандартный Reactstrap-рендер с правильными классами.
   - Убрать дублирующий `CarouselCaption`, либо заменить его на один аккуратный caption под/над слайдом, чтобы текст не повторялся.
   - Сохранить текущий публичный API: режим `items + renderItem` и режим `children`.

2. Исправить CSS для Carousel
   - Убрать зависимость от глобального селектора `.carousel-custom-control`, который сейчас не применяется через CSS Modules.
   - Добавить нормальные классы для контейнера, контролов и индикаторов через `styles.*`.
   - Проверить, что активным виден только один слайд, остальные скрыты.
   - Убедиться, что стрелки и точки находятся снизу и не ломают layout карточки демо.

3. Обновить пример на демо-странице
   - Оставить демонстрацию Carousel в `src/routes/index.tsx`, но убрать лишний caption из данных, если он больше не нужен как отдельная подпись.
   - Показать 3 слайда так, чтобы было очевидно, что компонент переключается корректно.

4. Проверить пакетные экспорты и сборку после правки
   - Проверить, что `Carousel` по-прежнему экспортируется из `src/lib/components/index.ts` и `src/lib/index.ts`.
   - Запустить проверку сборки библиотеки (`build:lib`) и демо-приложения (`build`).
   - При необходимости поправить типы/импорты, если сборка выявит ошибки.

Ожидаемый результат: на демо-странице в блоке Carousel будет виден только один активный слайд, переключатели будут работать, подписи не будут повторяться, а npm-библиотека продолжит собираться корректно.